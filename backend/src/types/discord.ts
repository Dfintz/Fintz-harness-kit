/**
 * Discord Bot Types
 *
 * Type definitions for Discord.js command interactions and options
 *
 * Note: Discord.js v14+ provides CommandInteractionOption<CacheType> which should
 * be used for typing interaction.options.data.
 *
 * Note: The canonical RateLimitResult contract lives in
 * services/shared/rateLimitPolicy.ts (the shared rate-limit taxonomy). The bot
 * tunnel limiter re-exports it as RateLimitResult from
 * bot/utils/tunnelRateLimiter.ts for back-compat.
 */
