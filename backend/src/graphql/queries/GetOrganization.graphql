"""
Example: Get organization details
"""
query GetOrganization($orgId: ID!) {
  organization(id: $orgId) {
    id
    name
    description
    members {
      id
      username
      role
    }
    fleets {
      id
      name
    }
  }
}
