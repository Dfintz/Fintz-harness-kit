"""
Example: Get user fleets with ships
This is an example query that demonstrates how to structure persisted queries.
"""
query GetUserFleets($userId: ID!) {
  user(id: $userId) {
    id
    username
    fleets {
      id
      name
      description
      ships {
        id
        name
        manufacturer
        role
      }
    }
  }
}
