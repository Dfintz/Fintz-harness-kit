interface:
  display_name: Prototype
  short_description: Build throwaway logic prototypes to validate state models and data shape.
policy:
  allow_implicit_invocation: false
  behavior_class: explicit-invocation-required
