interface:
  display_name: PR Workflow
  short_description: Pull request creation, verification, and review-before-ship workflow.
policy:
  allow_implicit_invocation: false
  behavior_class: explicit-invocation-required
