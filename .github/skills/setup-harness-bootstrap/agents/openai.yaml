interface:
  display_name: Setup Harness Bootstrap
  short_description: Adopt harness workflows, registry, and validation in new repositories.
policy:
  allow_implicit_invocation: false
  behavior_class: explicit-invocation-required
