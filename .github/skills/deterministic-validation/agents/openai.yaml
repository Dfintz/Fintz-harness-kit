interface:
  display_name: Deterministic Validation
  short_description: Define objective exit criteria and completion checks.
policy:
  allow_implicit_invocation: true
  behavior_class: model-invoked-eligible
