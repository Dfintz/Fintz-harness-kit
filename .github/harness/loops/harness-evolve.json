{
  "name": "harness-evolve",
  "category": "optimization",
  "kind": "experiment",
  "description": "Meta-evolution: improve the harness's own guidance (candidate-instructions.md) and KEEP an edit only if the eval score rises. Runs via scripts/harness/harness-evolve.mjs, which enforces eval-suite exclusion + integrity (autonomy OFF by default).",
  "purpose": "Continuously refine harness guidance based on observed loop outcomes (convergence speed, skill reuse, memory quality, routing accuracy)",
  "maxIterations": 5,
  "noImprovementStop": 3,
  "metric": {
    "name": "eval-harness-score",
    "run": "node scripts/harness/eval/run-eval.mjs --agent \"{{evolve.evalAgent}}\" --json",
    "extract": "\"harnessScore\"\\s*:\\s*([0-9.]+)",
    "direction": "maximize",
    "timeoutMs": 1800000
  },
  "target": [".github/harness/evolve/candidate-instructions.md"],
  "validation": {
    "splitStrategy": "component-count",
    "splitStrategyNote": "The first held_in.length confidence markers are assigned to the held-in group; the next held_out.length markers to the held-out group. Falls back to midpoint when N < requiredTotal.",
    "held_in": ["convergence_time", "skill_reuse"],
    "held_out": ["memory_quality", "routing_accuracy"],
    "threshold": 0.05,
    "rule": "delta_in >= 0 AND delta_out >= 0 AND max(delta_in, delta_out) > threshold",
    "fallback": "midpoint when N < held_in.length + held_out.length; single-score when N <= 2"
  },
  "runtime": {
    "agent": "gpt-5.3-codex",
    "modelsSeen": ["claude-opus-4.8"],
    "evaluator": "grade-trace",
    "guards": ["evolve-guard"]
  },
  "successCriteria": {
    "minimumScore": 0.5,
    "minimumIterations": 2,
    "maximumIterations": 8,
    "convergenceSignal": "netGain > 0.1 and stuckIsGood=false"
  },
  "metadata": {
    "releasePhase": "phase-3-5-integration",
    "dependencies": [
      "scripts/harness/evolve-guard.mjs",
      "scripts/harness/grade-trace.mjs",
      "scripts/harness/run-experiment.mjs"
    ],
    "blockedBy": [],
    "blocks": ["harness-report-integration"]
  },
  "skills": [],
  "instructions": [".github/instructions/04-IMPLEMENT.md"],
  "fixPrompt": "Improve the generic working guidance in .github/harness/evolve/candidate-instructions.md so an agent following it solves the eval tasks more reliably (cleaner fixes, sharper reviews, no metric-gaming). Edit ONLY that file. Keep the guidance generic and safe — do not encode task-specific answers, and do not reference, weaken, or mention the eval suite, verifiers, guardrails, or security files.",
  "guardrails": [
    "Edit ONLY .github/harness/evolve/candidate-instructions.md — the runner aborts if any forbidden path (eval suite, guardrails, memory, config, evolve machinery) changes.",
    "Never encode task-specific answers or fixtures into the guidance — that is reward-hacking the eval, not improving the harness.",
    "Never instruct disabling checks, lint, types, or adding suppressions.",
    "The eval suite scores you; you may not edit, read-for-answers, or weaken it."
  ],
  "onExhausted": "Stop. Report the baseline and best eval-harness-score, which guidance edits were kept vs reverted, and whether further gains need richer eval tasks rather than more prompt tuning."
}
