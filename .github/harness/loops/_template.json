{
  "name": "_template",
  "kind": "convergence",
  "description": "Copy this file to <name>.json. One sentence describing what 'done' means.",
  "maxIterations": 3,
  "checks": [
    {
      "name": "example-check",
      "run": "echo replace-me && exit 1",
      "timeoutMs": 300000
    }
  ],
  "rubric": [
    "Workflow loops only: list explicit, independently gradeable done-criteria here. Delete for convergence loops."
  ],
  "skills": [],
  "instructions": [],
  "fixPrompt": "Describe exactly what the agent should do with the failure output of each check. Reference the loaded skills for patterns. Fix root causes only.",
  "guardrails": [
    "Name the specific shortcuts this loop must never take to converge."
  ],
  "onExhausted": "Stop. Report the final state of every check, what was attempted in each iteration, and the suspected root cause. Do not retry.",
  "reflexionEnabled": false,
  "_reflexionEnabled_note": "Set to true to enable per-iteration Reflexion memory (Shinn et al. 2303.11366). When enabled, the fix prompt asks the agent to write TRIED/OUTCOME/HYPOTHESIS to a scratchpad file; that file is injected into the next iteration's prompt (wrapped as untrusted). Default: false — no behavior change to existing loops.",
  "waveBoundary": {
    "iterationsPerWave": 2,
    "summaryPrompt": "Workflow loops: summarize progress at iteration boundaries. (1) what passed? (2) what still fails? (3) pattern? (4) next strategy?"
  }
}
