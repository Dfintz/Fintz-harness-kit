# <Deliverable title>

## Summary

This version is deliberately flawed so the checks have something to catch (proving they bite).

## Body

It makes a claim that cites a source[^missing] which is never defined, and it omits the required
Sources section entirely. Both defects are caught by this pack's checks.
